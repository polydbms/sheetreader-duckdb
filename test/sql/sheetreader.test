# name: test/sql/sheetreader.test
# description: test sheetreader extension
# group: [sheetreader]

# Before we load the extension, this will fail
statement error
SELECT sheetreader('Sam');
----
Catalog Error: Scalar Function with name sheetreader does not exist!

# Require statement will ensure this test is run with this extension loaded
require sheetreader

# Confirm the extension works
query I
SELECT sheetreader('Sam');
----
Sheetreader Sam 🐥

query I
SELECT sheetreader_openssl_version('Michael');
----
<REGEX>:Sheetreader Michael, my linked OpenSSL version is OpenSSL.*